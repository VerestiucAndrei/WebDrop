rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Only allow access if the user is logged in as YOU
      allow read, write: if request.auth != null && request.auth.uid == 'YOUR_UNIQUE_ID_HERE';
    }
  }
}
